# Prompts

Structured-output extraction prompts for the Claude API pipeline (Sprint 2). Prompts live here — in the repo, versioned — with an eval harness against the golden dataset. Model or prompt changes must not silently change verdicts.

The LLM extracts; it never adjudicates. Pass/fail is deterministic rule evaluation only.
